<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobprep.jobprep_platform.mapper.CollectionMapper">

    <sql id="collectionColumns">
        collection_id,
        name,
        description,
        creator_id,
        created_at,
        updated_at
    </sql>

    <select id="findById" resultType="com.jobprep.jobprep_platform.model.entity.Collection">
        SELECT
            <include refid="collectionColumns"/>
        FROM collection
        WHERE collection_id = #{collectionId}
    </select>

    <select id="findByCreatorId" resultType="com.jobprep.jobprep_platform.model.entity.Collection">
        SELECT
            <include refid="collectionColumns"/>
        FROM collection
        WHERE creator_id = #{creatorId}
        ORDER BY created_at DESC, collection_id DESC
    </select>

    <select id="findByIdAndCreatorId" resultType="com.jobprep.jobprep_platform.model.entity.Collection">
        SELECT
            <include refid="collectionColumns"/>
        FROM collection
        WHERE collection_id = #{collectionId}
          AND creator_id = #{creatorId}
    </select>

    <select id="countByCreatorIdAndNoteId" resultType="integer">
        SELECT COUNT(*)
        FROM collection c
        INNER JOIN collection_note cn ON cn.collection_id = c.collection_id
        WHERE c.creator_id = #{creatorId}
          AND cn.note_id = #{noteId}
    </select>

    <insert id="insert" parameterType="com.jobprep.jobprep_platform.model.entity.Collection" useGeneratedKeys="true" keyProperty="collectionId">
        INSERT INTO collection (
            name,
            description,
            creator_id
        ) VALUES (
            #{name},
            #{description},
            #{creatorId}
        )
    </insert>

    <update id="update" parameterType="com.jobprep.jobprep_platform.model.entity.Collection">
        UPDATE collection
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            updated_at = NOW()
        </set>
        WHERE collection_id = #{collectionId}
    </update>

    <delete id="deleteById">
        DELETE FROM collection
        WHERE collection_id = #{collectionId}
    </delete>

</mapper>
